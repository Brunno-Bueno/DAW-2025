function inicio() {
    const message = "Estou iniciando...";
    console.log(message);
}
function process() {
    const mensagem = "Estou processando...";
    return mensagem;
}
inicio();
const processado = process();
console.log(processado);